import React from 'react';
import ReactDOM from 'react-dom/client';
import WebAPIViewer from './components/WebAPIViewer';

const root = ReactDOM.createRoot(document.getElementById('webapi-viewer-root')!);
root.render(<WebAPIViewer />);
